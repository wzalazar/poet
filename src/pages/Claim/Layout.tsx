import * as React from "react";
import { Link } from 'react-router';

import './style.scss';

export interface LandingProps {
}

export class ClaimLayout extends React.Component<LandingProps, undefined> {
  render() {
    return (
      <div className="claim">
      </div>
    )
  }
}
